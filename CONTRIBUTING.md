# Contributing

Testing locally:

```shell
asdf plugin test dolt https://github.com/glichten/asdf-dolt.git "dolt version"
```

Tests are automatically run in GitHub Actions on push and PR.

## Linting & Formatting

```shell
./scripts/lint.bash
./scripts/format.bash
```
